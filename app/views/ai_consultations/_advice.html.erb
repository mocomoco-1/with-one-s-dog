<% advice = ai_consultation.initial_response %>
<% if advice.present? %>
<div class="mt-2 p-2 bg-white rounded-lg border border-2 border-primary">
  <p><strong>悩みの内容:</strong> <%= ai_consultation.category %></p>
  <p><strong>いつ起こるか:</strong> <%= ai_consultation.situation %></p>
  <p><strong>愛犬の様子:</strong> <%= ai_consultation.dog_reaction %></p>
  <p><strong>どうなってほしいか:</strong> <%= ai_consultation.goal %></p>
  <p><strong>補足:</strong> <%= ai_consultation.details %></p>
</div>
<div class="mt-2 p-4 bg-white rounded-lg border border-2 border-secondary">
  <h2 class="font-bold text-xl flex items-center gap-2">
    <i class="fas fa-dog"></i>「TOMONIくん」の回答
  </h2>
  <div class="mt-2">
    <h3 class="text-lg font-semibold flex items-center gap-2">
      お悩みの要約🌿
    </h3>
    <p class="mt-2 leading-relaxed"><%= sanitize(advice["summary"]) %></p>
  </div>
  <div class="mt-2">
    <h3 class="text-lg font-semibold flex items-center gap-2">
      共感メッセージ🤝
    </h3>
    <p class="mt-2 leading-relaxed"><%= sanitize(advice["empathy"]) %></p>
  </div>
  <div class="mt-2">
    <h3 class="text-lg font-semibold flex items-center gap-2">
      短期的にできる工夫🎈
    </h3>
    <ul class="mt-2 list-disc list-inside space-y-2 leading-relaxed">
      <% advice["advice"]["short_term"]&.each do |advice| %>
      <li><%= sanitize(advice) %></li>
      <% end %>
    </ul>
    <h3 class="text-lg font-semibold flex items-center gap-2">
      長期的な改善ステップ🌷
    </h3>
    <ul class="mt-2 list-disc list-inside space-y-2 leading-relaxed">
      <% advice["advice"]["long_term"]&.each do |advice| %>
      <li><%= sanitize(advice) %></li>
      <% end %>
    </ul>
  </div>
  <div class="mt-2">
    <h3 class="text-lg font-semibold flex items-center gap-2">
      応援🏳‍🌈
    </h3>
    <p class="mt-2 leading-relaxed font-medium"><%= sanitize(advice["cheer"]) %></p>
  </div>
</div>
<% end %>